server.port= 9090

#to run on diff instance ex: port 9091, we can configure in above dropdown->edit configuration->
#Copy configuration, change the name->Modify Options->Add VM options->add -Dserver.port=9091

spring.application.name=question-service
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/questiondb
spring.datasource.username=postgres
spring.datasource.password=ib1234
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
